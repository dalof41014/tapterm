// Feature flags for in-progress features kept dark until verified end-to-end.
// Flip to true to re-enable. (tmux control-mode MVP compiles but is not yet
// runtime-tested against a real tmux server.)
export const TMUX_ENABLED = true;
